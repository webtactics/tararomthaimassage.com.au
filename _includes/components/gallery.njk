
<div class="photo-gallery">
  {% for folder in post.imageFolders %}
    {% for i in range(1, folder.count + 1) %}
      <img src="/static/img/treatments/{{ folder.name }}/{{ folder.name }}-{{ i }}.jpg" alt="Image {{ i }}">
    {% endfor %}
  {% endfor %}
</div>